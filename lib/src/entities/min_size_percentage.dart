/// Class for describing minimum size of the ad in percentage
final class MinSizePercentage {
  const MinSizePercentage({
    required this.width,
    required this.height,
  });

  /// Minimum width of the ad in percentage
  final int width;

  /// Minimum height of the ad in percentage
  final int height;
}
