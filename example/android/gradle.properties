org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Optional local-native testing: use -PaudienzzNativeVersion=0.3.0-local with Gradle,
# or add that property here temporarily. The default uses the published 0.3.0 artifact.
# See LOCAL_TESTING.md. Do not commit an active local override.
